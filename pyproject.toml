[project]
name = "provscope_observer"
description = "System-calls monitor and provenance trace generator for GNU/Linux"
version = "0.2.0"
authors = [
    { name = "PoneyUHC", email = "uhc.contact@laposte.net" }
]


[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]